apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mizzoucerivolume
spec:
  storageClassName: rook-cephfs-east
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
