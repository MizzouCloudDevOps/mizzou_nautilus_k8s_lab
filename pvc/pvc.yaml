apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: Your-Unique-Volume-Name
spec:
  storageClassName: rook-cephfs-east
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 200Mi
